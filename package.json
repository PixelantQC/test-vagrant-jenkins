{
  "name": "test-vagrant-jenkins",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node server",
    "test": "mocha",
    "ci-test": "istanbul cover mocha -- -R tap  \"test/*.js\" > test.tap && istanbul report clover"
  },
  "license": "ISC",
  "dependencies": {
    "chai": "^3.5.0",
    "express": "^4.13.4",
    "mocha": "^2.4.5",
    "supertest": "^1.2.0"
  },
  "devDependencies": {
    "istanbul": "^0.4.3"
  }
}
